{
  "name": "term-web",
  "version": "0.1.0",
  "private": true,
  "description": "Web-Terminal mit Sidebar (Standard-Shell + tmux-Sessions + URL-Parsing) fuer term.martuni.de",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "start": "node server.js",
    "dev": "node build.mjs && node server.js"
  },
  "dependencies": {
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/xterm": "^5.5.0",
    "esbuild": "^0.24.0"
  }
}
